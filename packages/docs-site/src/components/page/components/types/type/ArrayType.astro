---
import type { ArrayTypeModel, PageModel, BuildContext } from "docs-model";
import Type from "../Type.astro";
interface Props {
    type: ArrayTypeModel;
    context: BuildContext<PageModel>;
}
const { type, context } = Astro.props;
---

<Type context={context} type={type.componentType} location="array" />[]
