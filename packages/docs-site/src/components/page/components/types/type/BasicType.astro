---
import type { BasicTypeModel, BuildContext, PageModel} from "docs-model";
interface Props {
    type: BasicTypeModel;
    context: BuildContext<PageModel>;
}
const { type } = Astro.props;
---

<span>{type.displayName}</span>
