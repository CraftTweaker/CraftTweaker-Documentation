---
import type { BuildContext, ListItemCommentModel, PageModel } from "docs-model";
import Comments from "../Comments.astro";
interface Props {
    comment: ListItemCommentModel;
    context: BuildContext<PageModel>;
    flatten?: boolean;
}
const { comment, context } = Astro.props;
---

<li>
    <Comments context={context} comments={comment.content} />
</li>
