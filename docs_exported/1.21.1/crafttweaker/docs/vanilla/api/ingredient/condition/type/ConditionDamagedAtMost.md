# ConditionDamagedAtMost

## Importing the class

It might be required for you to import the package if you encounter any issues (like casting an Array), so better be safe than sorry and add the import at the very top of the file.
```zenscript
import crafttweaker.api.ingredient.condition.type.ConditionDamagedAtMost;
```


## Extending Record

ConditionDamagedAtMost extends Record. That means all methods available in Record are also available in ConditionDamagedAtMost

## Implemented Interfaces
ConditionDamagedAtMost implements the following interfaces. That means all methods defined in these interfaces are also available in ConditionDamagedAtMost

- [IIngredientCondition](/vanilla/api/ingredient/condition/IIngredientCondition)

